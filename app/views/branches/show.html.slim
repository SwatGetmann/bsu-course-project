= render "branch", branch: @branch
p = link_to "New Branch", new_project_branch_path(@project)
p = link_to "New Branch (Copy this one)", branch_copy_path(@branch)
p = link_to "New Commit", new_branch_commit_path(@branch)
